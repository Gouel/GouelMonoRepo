{% extends 'base.j2' %}
{% block title %}payment - Gouel Front{% endblock %}

{% block content %}
<div class="container mx-auto p-4 text-center">
    {% if action == 'error' %}
    <!-- Affichage pour l'erreur -->
    <i class="iconoir-warning-triangle text-red-500 text-9xl"></i> <!-- Taille de l'icône agrandie -->
    <h2 class="text-4xl font-bold mb-4 text-red-500">Erreur de Paiement</h2> <!-- Taille du titre agrandie -->
    <div class="p-4 border-2 border-red-500 rounded-lg inline-block" bg-gray-900> <!-- Cadre pour le message -->
        <p class="text-lg text-gray-100">Une erreur est survenue lors du traitement de votre paiement. Veuillez
            réessayer.</p>

        <!-- Message d'indication --->
        <p class="text-m text-gray-100">
            HelloAsso peut prendre du temps à enregistrer le paiement.
            Cliquez sur le bouton
            <strong>Essayer à nouveau</strong> pour vérifier à nouveau le paiement. (Vous ne serez pas débité deux
            fois.)
        </p>


        <div class="flex justify-center mt-4">
            <a href="#" onclick="window.location.reload();"
                class="border-b-4 bg-purple-600 border-purple-800 hover:bg-purple-500 hover:border-purple-700  text-white font-bold py-2 px-4 rounded transition duration-300">
                Essayer à nouveau
            </a>
            <a href="{{backUrl}}"
                class="border-b-4 bg-purple-600 border-purple-800 hover:bg-purple-500 hover:border-purple-700  text-white font-bold py-2 px-4 rounded transition duration-300">
                Retourner sur la page précédente
            </a>
        </div>
    </div>
    {% elif action == 'refused' %}
    <!-- Affichage pour le paiement refusé -->
    <i class="iconoir-warning-circle text-yellow-500 text-9xl"></i>
    <h2 class="text-4xl font-bold mb-4 text-yellow-500">Paiement Refusé</h2>
    <div class="p-4 border-2 border-yellow-500 rounded-lg inline-block bg-gray-900">
        <p class="text-lg text-gray-100">Votre paiement a été refusé. Veuillez vérifier vos informations de paiement et
            réessayer.</p>

        <div class="flex justify-center mt-4">
            <a href="{{backUrl}}"
                class="border-b-4 bg-purple-600 border-purple-800 hover:bg-purple-500 hover:border-purple-700 text-white font-bold py-2 px-4 rounded transition duration-300">
                Essayer à nouveau
            </a>
        </div>
    </div>
    {% elif action == 'succeeded' %}
    <!-- Affichage pour le paiement accepté -->
    <i class="iconoir-check-circle text-green-500 text-9xl"></i>
    <h2 class="text-4xl font-bold mb-4 text-green-500">Paiement Accepté</h2>
    <div class="p-4 border-2 border-green-500 rounded-lg inline-block bg-gray-900">
        <p class="text-lg text-gray-100">Votre paiement a été accepté avec succès. Merci pour votre achat!</p>
        <div class="flex justify-center mt-4">
            <a href="{{url_for('main.solde')}}"
                class="border-b-4 bg-purple-600 border-purple-800 hover:bg-purple-500 hover:border-purple-700  text-white font-bold py-2 px-4 rounded transition duration-300">
                Voir mon solde
            </a>
        </div>
    </div>
    {% endif %}
</div>
{% endblock %}